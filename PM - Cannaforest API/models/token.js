const fakeBDD = require('../BDD/bdd');
const utils = require('../utils/utils');

const characters ='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+=-_&*$ۣ#@.})]%^';

class Token{
    generateToken(){ 
    }
    
    hashToken(){     
    }

    getHashToken(){ 
    }
}

const token = new Token();

module.exports = token;